spring:
  shell.interactive.enabled: false

  messages:
    encoding: cp1251
    basename: library

  data:
    mongodb:
      port: 0
      database: test
      #uri: mongodb://localhost
      #port: 27017
      #database: springdb

logging:
  level:
    root: ERROR